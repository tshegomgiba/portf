# Web3Forms access key for the contact form.
# Get one free at https://web3forms.com, then paste it here for local `npm run dev`.
# Production builds (including Vercel) read `.env.production` instead.
VITE_WEB3FORMS_KEY=
